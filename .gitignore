# --- GERAL / SISTEMA OPERACIONAL ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# --- BACKEND (PYTHON) ---
# Bytecode compilado (não serve para outros computadores)
__pycache__/
*.pyc

# Ambientes Virtuais (PESADO - Nunca commitar)
venv/
.venv/
env/

# Variáveis de Ambiente (SEGURANÇA - Nunca commitar senhas)
.env
backend/.env

# Banco de dados local (se usar SQLite)
*.sqlite3
*.db

# --- FRONTEND (NEXT.JS / NODE) ---
# Dependências (GIGANTE - Nunca commitar)
node_modules/

# Builds de produção e cache do Next.js
.next/
build/
dist/
.cache/

# Variáveis de ambiente locais
.env.local
.env.development.local
.env.test.local
.env.production.local
frontend/.env.local

# Logs de erro npm/yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*